const int A = 5;

func conteAArray(char[A] lletres, char caracter) : boolean begin
    int b = 0;
    int char = 0;
    int a = c + 2;

    boolean trobat = false;
    while ('a') do

    end;

    string line = "Hola";

    int i = 0;
    while (i < A and not trobat) do
        if lletres[i] == caracter do
            trobat = true;
        end;
    end;

    return trobat;
end conteAArray;


/*
proc main() begin
    const char[A] vocals = char{'a', 'e', 'i', 'o', 'u'};

    string entrada;
    int length = 0;
    int i = 0; 

    int numVocals = 0;

    while (i < length) do
        numVocals = numVocals + 1;
        i++;
    end;
end main;
*/