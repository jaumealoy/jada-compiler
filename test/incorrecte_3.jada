//S'intenta modificar una constant (NO PROVAT)
const int A = 6;

func set0(const int[A] arr) : boolean begin
    for (int i = 0; i < A; i++) do
        arr[i] = 0;
    end;
    return true;
end llegirEnter;


proc main() begin
    const int[A] array = int{11, 0, 3, 9, 21, 0};
	set0(array);
end main;
