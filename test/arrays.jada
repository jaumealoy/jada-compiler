func llegirEnter() : int begin
    string input = readLine();
    int tmp = 1;

    for(int i = 0; i < input.length; i++) do
        char c = input[i];
    end;

    return 453;
end llegirEnter;

// Entrades TS
// 50_50_50_int_array dtipus ts_array int
// matriu dvar 50_50_50_int_array

proc main() begin
    int[50][50][50] matriu;

    for(int i = 0; i < 3; i++) do
        for(int j = 0; j < 3; j++) do
            matriu[i][j] = llegirEnter();
        end;
    end;
end main;

// Entrades TS
// transposa dproc ----> m 50_50_int_array -----> dimensions int

proc transposa(int[50][50] m, int dimensions) begin

end transposa;