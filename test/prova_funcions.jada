/**
  * Calcula de manera recursiva el factorial d'un nombre enter positiu
  * n: enter positiu
  * return: enter positiu
  */
func factorial(int n) : int begin
    if n == 1 do
        // cas base
        return 1;
    else do
        // crides recursives
        return factorial(n - 1);
    end;
end factorial;

/**
  * Calcula la suma d'un vector de longitud n
  * return: enter
  */
func suma(int[128] v, int n) : int begin 
    int tmp = 0;

     
    // for (int i = 0; i < n; i++) do
    //    tmp = tmp + v[i];    
    // end;
    
end suma;

func provaError(int v, char v) : int begin

end provaError;