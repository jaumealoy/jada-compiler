/*
1. Corregir error sintàctic "else" a "else do". Mirau missatge d'error. (Missatges d'error segons lo esperat).
2. Corregir error de tipus de retorn. (Control tipus  retorn).
3. Posar parámetres a la crida "suma". (Control parámetres d'entrada)
*/

int contadorGlobal = 0;

func tornaBool() : boolean begin
    contadorGlobal++;
    if (contadorGlobal == 3) do
        return 3;
    else do
        return false;
    end;
end tornaBool;

func tornaInt() : int begin
    return 2;
end tornaInt;

func suma(int a, int b) : int begin
    return (a+b);
end suma;

proc main() begin
    while (tornaBool()) do
        if (tornaInt()) do
            suma();
        end;
    end;
    main();
end main;