/**
 *Procediments i funcions sense sentit però rebuscades que respecten la gramàtica.
 */

func function() : int begin
    boolean b;
    if (b) do
        return 1;
    else do
        return 2;
    end;
end function;

proc main() begin
int a, b;

for (char c = 'a'; true ; b += 3+ 2) do
    if ( ( ( true ) ) ) do
        break;
    else do
        b = function();
    end;
end;
end main;

